import { Text, View } from "react-native";

export default function MapScreen() {
  return (
    <View style={{flex:1,justifyContent:"center",alignItems:"center"}}>
      <Text>Maps available only on mobile</Text>
    </View>
  );
}